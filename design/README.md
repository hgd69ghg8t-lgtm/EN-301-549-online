# design/

Drop Figma exports here (PNG/SVG screenshots of frames, or exported design tokens as JSON/CSS) as you design.

Once files are added and pushed to the `claude/pdf-accessible-website-j88o8l` branch, let Claude know and it'll pull the new design direction into `docs/assets/css/style.css` and the page template, then re-verify WCAG 2.2 AA contrast and re-check the affected pages.
